case: valuable_recent_record_preserved
summary: Keep a recent useful durable record active.

expected:
  must_use_tools:
    - list_records
  must_not_use_tools:
    - archive_record
    - supersede_record
  record_text_must_include_all:
    - provider
    - adapter
    - stable contract
