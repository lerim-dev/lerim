case: no_change_when_store_is_already_clean
summary: Leave an already-clean store unchanged.

expected:
  must_use_tools:
    - list_records
  must_not_use_tools:
    - archive_record
    - supersede_record
    - update_record
  preserved_titles:
    - Keep context and session state in separate stores
    - Normalize provider events at the adapter boundary
